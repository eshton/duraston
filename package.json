{
  "name": "duraston",
  "version": "0.0.0",
  "private": true,
  "description": "Exploration: running astonagent agents on Golem's durable-execution runtime (WebAssembly workers + oplog).",
  "license": "UNLICENSED",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  }
}
