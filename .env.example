# Copy to .env and fill in. Never commit the real .env.
# Used by future capability integrations (LLM + tools); not required to build.

# Anthropic API key for LLM-backed triage/research (see ARCHITECTURE.md item 3)
ANTHROPIC_API_KEY=

# Golem deployment target (when deploying to a remote cluster rather than local)
# GOLEM_PROFILE=local
