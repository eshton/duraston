{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "types": []
  },
  "include": ["src/**/*.ts"],
  "//exclude": "src/golem is the Golem deploy target; it imports @golemcloud/golem-ts-sdk and only compiles under the Golem build pipeline (DUR-9), so it is excluded from the local tsc.",
  "exclude": ["src/golem/**"]
}
